const myName: string = 'Jakir'
const fruits: string[] = ['mango', 'banana', 'jackfruit', 'apple']
type Person = { name: string, age: number, salary: number, isGoodBoy: boolean }
const friends: Person = {
    name: 'Abul',
    age: 23,
    salary: 90000,
    isGoodBoy: true
};
const add2 = (num1: number, num2: number): number => num1 + num2;
const addArryFunction = (num1: number, num2: number): void => {
    const sum2 = num1 + num2;
}
addArryFunction(50, 50)